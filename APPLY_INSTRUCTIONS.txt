HUMAN? V2.3 — Machine Twin Evolution — PATCH ONLY

This archive intentionally contains ONLY the V2.3 functional code/test files.
It does NOT replace your V2.2 project, Vite config, GitHub Pages config, package files, Prediction/Mirror code, or existing tests.

Files included:
- src/visuals/MachineTwinProfile.ts          (new)
- src/visuals/MachineTwinCanvas.tsx          (replace existing)
- src/scenes/ResultScene.tsx                 (replace existing)
- src/__tests__/MachineTwinProfile.test.ts   (new)

Why docs are not included:
The Gemini V2.3 project was built on an older 48-test V2.2 branch. Its README/DEVELOPMENT_STATUS test counts therefore do not match the protected current baseline. Keeping the current documentation avoids reintroducing stale baseline claims; docs can be updated after local verification.

Expected protected-baseline result after applying this patch:
- Existing V2.2 tests: 52
- New MachineTwinProfile tests: 12
- Expected total: 64 tests across 8 test files

After copying into the project root with Replace/Overwrite, run:
  npm run typecheck
  npm run test
  npm run build

Do NOT copy vite.config.ts from the Gemini full ZIP. Your current project must retain base: '/HUMAN/' for GitHub Pages.
Do NOT copy the full Gemini ZIP over the project.
